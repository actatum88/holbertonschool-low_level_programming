#include <stdlib.h>
#include <stdio.h>

/**
 * main - Print its name
 *@argc: arguments
 *@argv: array pointing to arguments
 *
 *Return: Always 0.
 */
int main(int argc, char **argv)
{
	int i, res = 1;

	if (argc == 3)
	{
		for (i = 1; i < argc; i++);
		res = atoi(argv[1]) * atoi(argv[2]);
	}
		printf("%d\n");
	else
	{
		printf("%s\n", "Error");
		return (1);
	}
return (0);
}
