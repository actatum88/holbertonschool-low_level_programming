#include <stdio.h>

/**
 * main - Task 2 to play alphabet game
 *
 * Return: Always 0 (Success)
 */
int main (void)
{
  char x;

  for (x = 'a' ; a <= 'z'; x++)
    {
      putchar(x);
    }
  putchar ('\n');

  return (0);
}
